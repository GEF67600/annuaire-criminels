{
  "name": "annuaire-criminel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "eleventy && pagefind --site _site --output-subdir pagefind",
    "start": "eleventy --serve",
    "serve-build": "npx serve _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "pagefind": "^1.1.0"
  }
}
